{% if article.tags.size > 0 %}
<span>{{ 'blogs.article.tags' | t }} :</span>
{% for tag in article.tags %}
<span><a href="{{ blog.url }}/tagged/{{ tag | handle }}">{{ tag | join: ', ' }}</a> {% unless forloop.last %},{% endunless %}</span>
{% endfor %}
{% endif %}